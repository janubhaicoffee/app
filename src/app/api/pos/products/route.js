import { supabaseAdmin } from "@/lib/supabaseAdmin";import { NextResponse } from "next/server";export async function GET(request) {  try {    const { searchParams } = new URL(request.url);    const outletId = searchParams.get("outletId");    const categoryId = searchParams.get("categoryId");    let query = supabaseAdmin.from("pos_products")      .select("*, pos_categories(name)")      .order("sort_order", { ascending: true })      .order("name", { ascending: true });    if (outletId) query = query.eq("outlet_id", outletId);    if (categoryId) query = query.eq("category_id", categoryId);    const { data, error } = await query;    if (error) throw error;    return NextResponse.json({ success: true, data });  } catch (error) {    console.error("POS Products GET error:", error);    return NextResponse.json({ error: error.message }, { status: 500 });  }}export async function POST(request) {  try {    const body = await request.json();    const { outlet_id, category_id, name, price, cost, current_stock, sku, barcode, image_url, sort_order, modifiers, is_available, source_product_id, commission_per_unit } = body;    if (!outlet_id || !name || price === undefined) {      return NextResponse.json({ error: "Missing required fields: outlet_id, name, price" }, { status: 400 });    }    const parsedPrice = parseFloat(price);    if (isNaN(parsedPrice) || parsedPrice < 0) {      return NextResponse.json({ error: "Invalid price" }, { status: 400 });    }    const { data, error } = await supabaseAdmin      .from("pos_products")      .insert([{        outlet_id,        category_id: category_id || null,        name,        price: parsedPrice,        cost: cost !== undefined ? parseFloat(cost) : null,        current_stock: current_stock !== undefined ? parseInt(current_stock) : 0,        sku: sku || null,        barcode: barcode || null,        image_url: image_url || null,        sort_order: sort_order !== undefined ? parseInt(sort_order) : 0,        modifiers: modifiers || null,        is_available: is_available !== undefined ? !!is_available : true,        source_product_id: source_product_id || null,        commission_per_unit: commission_per_unit !== undefined ? parseFloat(commission_per_unit) : null      }])      .select()      .single();    if (error) throw error;    return NextResponse.json({ success: true, data }, { status: 201 });  } catch (error) {    console.error("POS Products POST error:", error);    return NextResponse.json({ error: error.message }, { status: 500 });  }}export async function PATCH(request) {  try {    const body = await request.json();    const { id, name, price, cost, current_stock, is_available, category_id, sku, barcode, image_url, sort_order, source_product_id, commission_per_unit } = body;      if (!id) {       return NextResponse.json({ error: "Missing product id" }, { status: 400 });     }      const updates = {};     if (name !== undefined) updates.name = name;     if (category_id !== undefined) updates.category_id = category_id;     if (price !== undefined) {       const p = parseFloat(price);       if (isNaN(p) || p < 0) return NextResponse.json({ error: "Invalid price" }, { status: 400 });       updates.price = p;     }     if (cost !== undefined) updates.cost = parseFloat(cost);     if (current_stock !== undefined) updates.current_stock = parseInt(current_stock);     if (sku !== undefined) updates.sku = sku;     if (barcode !== undefined) updates.barcode = barcode;     if (image_url !== undefined) updates.image_url = image_url;     if (sort_order !== undefined) updates.sort_order = parseInt(sort_order);     if (is_available !== undefined) updates.is_available = !!is_available;     if (source_product_id !== undefined) updates.source_product_id = source_product_id;     if (commission_per_unit !== undefined) updates.commission_per_unit = commission_per_unit !== null ? parseFloat(commission_per_unit) : null;    const { data, error } = await supabaseAdmin      .from("pos_products")      .update(updates)      .eq("id", id)      .select()      .single();    if (error) throw error;    return NextResponse.json({ success: true, data });  } catch (error) {    console.error("POS Products PATCH error:", error);    return NextResponse.json({ error: error.message }, { status: 500 });  }}