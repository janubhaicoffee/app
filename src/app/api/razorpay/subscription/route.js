import Razorpay from 'razorpay';import { NextResponse } from 'next/server';export async function POST(req) {  try {    const { amount, frequency } = await req.json();    if (!process.env.NEXT_PUBLIC_RAZORPAY_KEY_ID || !process.env.NEXT_SECRET_RAZORPAY_KEY) {      return NextResponse.json({ error: "Razorpay keys are missing" }, { status: 500 });    }    const razorpay = new Razorpay({      key_id: process.env.NEXT_PUBLIC_RAZORPAY_KEY_ID,      key_secret: process.env.NEXT_SECRET_RAZORPAY_KEY,    });    // 1. Create a dynamic plan for the specific cart amount    // Razorpay requires amount in paise.    const plan = await razorpay.plans.create({      period: frequency, // "weekly" or "monthly"      interval: 1,      item: {        name: `Coffee Subscription - ${frequency}`,        amount: amount * 100,        currency: "INR",        description: "Dynamic cart total subscription"      }    });    // 2. Create a subscription tied to this plan    const subscription = await razorpay.subscriptions.create({      plan_id: plan.id,      customer_notify: 1,      total_count: frequency === "weekly" ? 52 : 12, // Subscribes them for 1 year    });    return NextResponse.json({      subscriptionId: subscription.id,      planId: plan.id    });  } catch (error) {    console.error("Razorpay Subscription API Error:", error);    return NextResponse.json({ error: error.message || "Failed to create subscription" }, { status: 500 });  }}