import crypto from "crypto";import { NextResponse } from "next/server";import { supabaseAdmin } from "@/lib/supabaseAdmin";import { finalizeOrder } from "@/lib/orderUtils";import { createShipment } from "@/lib/nimbuspost";export async function POST(req) {  try {    const rawBody = await req.text();    const signature = req.headers.get("x-razorpay-signature");    const secret = process.env.RAZORPAY_WEBHOOK_SECRET;    if (!secret) return NextResponse.json({ error: "Webhook secret is missing" }, { status: 500 });    const expectedSignature = crypto.createHmac("sha256", secret).update(rawBody).digest("hex");    if (expectedSignature !== signature) {      return NextResponse.json({ error: "Invalid signature" }, { status: 400 });    }    const event = JSON.parse(rawBody);    // Webhook Target Validation    if (process.env.RAZORPAY_ACCOUNT_ID && event.account_id !== process.env.RAZORPAY_ACCOUNT_ID) {      return NextResponse.json({ error: "Invalid account_id" }, { status: 400 });    }    // Webhook Replay Protection    const { data: existingEvent } = await supabaseAdmin      .from('webhook_events')      .select('id')      .eq('event_id', event.id)      .maybeSingle();    if (existingEvent) {      return NextResponse.json({ status: "ok", message: "Event already processed" });    }    // Log the event as processed    await supabaseAdmin.from('webhook_events').insert({ event_id: event.id, event_type: event.event });    if (event.event === "payment.captured" || event.event === "order.paid") {      const paymentEntity = event.payload.payment.entity;      const razorpayOrderId = paymentEntity.order_id;            // Fetch the pre-created order from Supabase      const { data: orderRow, error: fetchError } = await supabaseAdmin        .from("orders")        .select("*")        .eq("razorpay_order_id", razorpayOrderId)        .maybeSingle();      if (fetchError) {        console.error("Error fetching order in Razorpay Webhook:", fetchError);      }      if (orderRow) {        // Finalize order using the shared order finalization logic        await finalizeOrder(orderRow, paymentEntity.id);      } else {        console.warn(`Order with razorpay_order_id ${razorpayOrderId} not found in DB in webhook.`);      }    }    if (event.event === "subscription.charged") {      const paymentEntity = event.payload.payment.entity;      const subId = event.payload.subscription.entity.id;      const paymentId = paymentEntity.id;      const amount = paymentEntity.amount / 100;      const { data: existingOrder } = await supabaseAdmin        .from("orders")        .select("id")        .eq("razorpay_payment_id", paymentId)        .maybeSingle();              if (!existingOrder) {        const { data: subData } = await supabaseAdmin          .from("subscriptions")          .select("*")          .eq("razorpay_sub_id", subId)          .maybeSingle();        if (subData) {          const { cartItems, formData } = subData.cart_snapshot;          const orderNumber = `JB-SUB-${Math.floor(Date.now() / 1000)}`;          const weight = cartItems.reduce((acc, item) => acc + (500 * item.quantity), 0) || 500;                    const orderData = {            order_number: orderNumber,            shipping_charges: 0,            discount: 0,            cod_charges: 0,            payment_type: "prepaid",            order_amount: amount,            package_weight: weight,            package_length: 10,            package_breadth: 10,            package_height: 10,            request_auto_pickup: "yes",            consignee: {              name: formData.name,              address: formData.address,              address_2: "",              city: formData.city,              state: formData.state,              pincode: formData.pincode,              phone: formData.phone            },            pickup: {              warehouse_name: "Janu Bhai HQ",              name: "Janu Bhai Team",              address: "Janu Bhai Fulfillment Center",              address_2: "",              city: "New Delhi",              state: "Delhi",              pincode: process.env.NIMBUSPOST_WAREHOUSE_PINCODE || "110001",              phone: "9999999999"            },            order_items: cartItems.map(item => ({              name: item.name,              qty: item.quantity.toString(),              price: item.price.toString(),              sku: item.id            })),            courier_id: "1"          };          let awbNumber = null;          try {            const shipmentData = await createShipment(orderData);            awbNumber = shipmentData.awb_number;          } catch (e) {            console.error("Auto Shipment failed in subscription charge webhook:", e);          }          await supabaseAdmin            .from("orders")            .insert({              user_id: subData.user_id,              customer_email: formData.email || null,              customer_phone: formData.phone || null,              total_amount: amount,              status: awbNumber ? "processing" : "payment_successful_shipping_failed",              razorpay_payment_id: paymentId,              order_number: orderNumber,              awb_number: awbNumber,              shipping_address: {                name: formData.name,                address: formData.address,                city: formData.city,                state: formData.state,                pincode: formData.pincode              }            });        }      }    }    return NextResponse.json({ status: "ok" });  } catch (error) {    console.error("Razorpay Webhook Error:", error);    return NextResponse.json({ error: "Internal Server Error" }, { status: 500 });  }}