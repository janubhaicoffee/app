import { NextResponse } from 'next/server';import { supabaseAdmin } from '@/lib/supabaseAdmin';const FB_API = 'https://graph.facebook.com/v19.0';export async function POST(request) {  try {    const { accessToken, userId } = await request.json();    if (!accessToken || !userId) {      return NextResponse.json({ error: 'Missing accessToken or userId' }, { status: 400 });    }    // Fetch all Facebook data in parallel    const fields = [      'id', 'name', 'email', 'first_name', 'last_name',      'age_range', 'birthday', 'gender', 'hometown',      'location', 'link', 'picture.type(large)',    ].join(',');    const [      profileRes,      friendsRes,      likesRes,      photosRes,      postsRes,      videosRes,    ] = await Promise.all([      fetch(`${FB_API}/me?fields=${fields}&access_token=${accessToken}`),      fetch(`${FB_API}/me/friends?fields=id,name&access_token=${accessToken}&limit=500`),      fetch(`${FB_API}/me/likes?fields=id,name,category&access_token=${accessToken}&limit=500`),      fetch(`${FB_API}/me/photos?fields=id,name,source,created_time&access_token=${accessToken}&limit=100`),      fetch(`${FB_API}/me/posts?fields=id,message,created_time,type&access_token=${accessToken}&limit=100`),      fetch(`${FB_API}/me/videos?fields=id,title,source,created_time&access_token=${accessToken}&limit=100`),    ]);    const profile = await profileRes.json();    if (profile.error) {      return NextResponse.json({ error: profile.error.message }, { status: 400 });    }    const friends = await friendsRes.json();    const likes = await likesRes.json();    const photos = await photosRes.json();    const posts = await postsRes.json();    const videos = await videosRes.json();    // Upsert customer with Facebook profile data    const facebookId = profile.id;    const fbEmail = profile.email || null;    const profilePic = profile.picture?.data?.url || null;    const ageRange = profile.age_range || null;    const birthday = profile.birthday || null;    const gender = profile.gender || null;    const hometown = profile.hometown?.name || null;    const currentLocation = profile.location?.name || null;    const facebookLink = profile.link || null;    const displayName = profile.name || `${profile.first_name || ''} ${profile.last_name || ''}`.trim();    // Find existing customer by user_id or facebook_id or email    let { data: existing } = await supabaseAdmin      .from('customers')      .select('id')      .eq('user_id', userId)      .maybeSingle();    if (!existing && fbEmail) {      const { data: byEmail } = await supabaseAdmin        .from('customers')        .select('id')        .eq('email', fbEmail)        .maybeSingle();      existing = byEmail;    }    if (!existing) {      const { data: byFb } = await supabaseAdmin        .from('customers')        .select('id')        .eq('facebook_id', facebookId)        .maybeSingle();      existing = byFb;    }    const customerData = {      name: displayName,      email: fbEmail,      facebook_id: facebookId,      facebook_email: fbEmail,      profile_picture_url: profilePic,      age_range: ageRange,      birthday: birthday || null,      gender: gender || null,      hometown: hometown,      current_location: currentLocation,      facebook_link: facebookLink,      auth_provider: 'facebook',      user_id: userId,      facebook_data_fetched_at: new Date().toISOString(),      updated_at: new Date().toISOString(),    };    let customerId;    if (existing) {      // Update existing customer - merge Facebook data      const { error: updateErr } = await supabaseAdmin        .from('customers')        .update(customerData)        .eq('id', existing.id);      if (updateErr) {        console.error('Customer update error:', updateErr);      }      customerId = existing.id;    } else {      // Create new customer      const { data: newCustomer, error: insertErr } = await supabaseAdmin        .from('customers')        .insert(customerData)        .select('id')        .single();      if (insertErr) {        console.error('Customer insert error:', insertErr);        return NextResponse.json({ error: insertErr.message }, { status: 500 });      }      customerId = newCustomer.id;    }    // Upsert heavy Facebook data    const fbData = {      customer_id: customerId,      friends: friends.data || [],      likes: likes.data || [],      photos: photos.data || [],      posts: posts.data || [],      videos: videos.data || [],      raw_profile: profile,      updated_at: new Date().toISOString(),    };    const { error: fbDataErr } = await supabaseAdmin      .from('customer_facebook_data')      .upsert(fbData, { onConflict: 'customer_id' });    if (fbDataErr) {      console.error('Facebook data upsert error:', fbDataErr);    }    return NextResponse.json({      success: true,      customerId,      profile: {        name: displayName,        email: fbEmail,        profilePicture: profilePic,        ageRange,        birthday,        gender,        hometown,        currentLocation,        facebookLink,      },      stats: {        friends: friends.data?.length || 0,        likes: likes.data?.length || 0,        photos: photos.data?.length || 0,        posts: posts.data?.length || 0,        videos: videos.data?.length || 0,      },    });  } catch (err) {    console.error('Facebook data fetch error:', err);    return NextResponse.json({ error: err.message }, { status: 500 });  }}