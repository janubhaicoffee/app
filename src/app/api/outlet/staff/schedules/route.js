import { supabaseAdmin } from "@/lib/supabaseAdmin";import { NextResponse } from "next/server";export async function GET(request) {  try {    const { searchParams } = new URL(request.url);    const outletId = searchParams.get("outletId");    const staffId = searchParams.get("staffId");    const startDate = searchParams.get("startDate");    const endDate = searchParams.get("endDate");    let query = supabaseAdmin.from("staff_attendance").select("date, clock_in, clock_out, total_hours, status").order("date", { ascending: false });    if (outletId) query = query.eq("outlet_id", outletId);    if (staffId) query = query.eq("staff_id", staffId);    if (startDate) query = query.gte("date", startDate);    if (endDate) query = query.lte("date", endDate);    const { data, error } = await query;    if (error) throw error;    return NextResponse.json({ success: true, data });  } catch (error) {    console.error("Staff Schedules GET error:", error);    return NextResponse.json({ error: error.message }, { status: 500 });  }}export async function POST(request) {  try {    const body = await request.json();    const { outlet_id, staff_id, date, shift, start_time, end_time, notes } = body;    if (!outlet_id || !staff_id || !date) {      return NextResponse.json({ error: "Missing required fields: outlet_id, staff_id, date" }, { status: 400 });    }    const insertData = {      outlet_id,      staff_id,      date,      status: "scheduled",      notes: notes || `Shift: ${shift || "regular"}, ${start_time || ""}-${end_time || ""}`,    };    const { data, error } = await supabaseAdmin      .from("staff_attendance")      .insert([insertData])      .select()      .single();    if (error) throw error;    return NextResponse.json({ success: true, data }, { status: 201 });  } catch (error) {    console.error("Staff Schedules POST error:", error);    return NextResponse.json({ error: error.message }, { status: 500 });  }}