import { NextResponse } from "next/server";import { supabaseAdmin } from "@/lib/supabaseAdmin";import { processDeliveryOrder } from "@/lib/deliveryUtils";export async function GET(request) {  try {    const { searchParams } = new URL(request.url);    const partner = searchParams.get("partner");    let query = supabaseAdmin      .from("outlet_delivery_orders")      .select("*")      .order("created_at", { ascending: false });    if (partner && partner !== "all") {      query = query.eq("partner", partner.toLowerCase());    }    const { data, error } = await query;    if (error) throw error;    return NextResponse.json({ success: true, data: data || [] });  } catch (error) {    console.error("Integrations Orders GET error:", error);    return NextResponse.json({ error: error.message }, { status: 500 });  }}let orderQueue = Promise.resolve();export async function POST(request) {  try {    const body = await request.json();    const { partner, items, price, total, couponUsed, customerName } = body;    const orderTotal = price || total || 0;    return new Promise((resolve) => {      orderQueue = orderQueue.then(async () => {        try {          await processDeliveryOrder({            partner: partner || "unknown",            items: items || [],            total: orderTotal,            couponUsed: couponUsed || null,            customerName: customerName || "Delivery Customer"          });          resolve(NextResponse.json({ success: true }));        } catch (err) {          resolve(NextResponse.json({ error: err.message }, { status: 500 }));        }      });    });  } catch (error) {    console.error("Integrations Orders POST error:", error);    return NextResponse.json({ error: error.message }, { status: 500 });  }}