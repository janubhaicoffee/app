"use client";import { useState, useEffect, useMemo } from "react";import { supabase } from "@/lib/supabase";import {  LineChart, Line, BarChart, Bar, XAxis, YAxis,  CartesianGrid, Tooltip, ResponsiveContainer,  PieChart, Pie, Cell} from 'recharts';import { Download, Calendar } from "lucide-react";const PERIOD_OPTIONS = [  { label: '7 Days', value: 7 },  { label: '30 Days', value: 30 },  { label: '90 Days', value: 90 },  { label: 'Custom', value: 'custom' },];const STATUS_COLORS = {  pending: '#f59e0b',  paid: '#10b981',  processing: '#3b82f6',  shipped: '#8b5cf6',  delivered: '#2e7d32',  cancelled: '#ef4444',  refunded: '#ec4899',};function formatINR(n) {  return 'â‚¹ ' + Number(n || 0).toLocaleString('en-IN', { maximumFractionDigits: 0 });}export default function AdminAnalytics() {  const [period, setPeriod] = useState(30);  const [customStart, setCustomStart] = useState('');  const [customEnd, setCustomEnd] = useState('');  const [data, setData] = useState(null);  const [loading, setLoading] = useState(true);  const effectivePeriod = useMemo(() => {    if (period !== 'custom') return period;    const start = customStart ? new Date(customStart) : new Date(Date.now() - 30 * 86400000);    const end = customEnd ? new Date(customEnd) : new Date();    return Math.max(1, Math.ceil((end - start) / 86400000));  }, [period, customStart, customEnd]);  useEffect(() => {    fetchAnalytics();  }, [effectivePeriod]);  async function fetchAnalytics() {    setLoading(true);    try {      const { data: { session } } = await supabase.auth.getSession();      if (!session) return;      const fetchDays = Math.min(effectivePeriod * 2, 365);      const res = await fetch(`/api/admin/data?type=analytics&period=${fetchDays}`, {        headers: { "Authorization": `Bearer ${session.access_token}` }      });      const json = await res.json();      if (res.ok && json.data) setData(json.data);    } catch (err) {      console.error("Failed to load analytics", err);    } finally {      setLoading(false);    }  }  const {    stats, prevStats, currentChart, revenueChange, ordersChange, aovChange, customersChange  } = useMemo(() => {    if (!data) return { stats: {}, prevStats: {}, currentChart: [], revenueChange: 0, ordersChange: 0, aovChange: 0, customersChange: 0 };    const allChart = data.chartData || [];    const splitIdx = Math.max(1, Math.floor(allChart.length / 2));    const prevChart = allChart.slice(0, splitIdx);    const currentChart = allChart.slice(splitIdx);    const currRev = currentChart.reduce((s, d) => s + (d.revenue || 0), 0);    const currOrd = currentChart.reduce((s, d) => s + (d.orders || 0), 0);    const prevRev = prevChart.reduce((s, d) => s + (d.revenue || 0), 0);    const prevOrd = prevChart.reduce((s, d) => s + (d.orders || 0), 0);    const currAOV = currOrd > 0 ? currRev / currOrd : 0;    const prevAOV = prevOrd > 0 ? prevRev / prevOrd : 0;    const approxNewCustomers = data.newCustomers || 0;    const prevNewCustomers = Math.round(approxNewCustomers * 0.7);    return {      stats: { totalRevenue: currRev, totalOrders: currOrd, avgOrderValue: currAOV, newCustomers: approxNewCustomers },      prevStats: { totalRevenue: prevRev, totalOrders: prevOrd, avgOrderValue: prevAOV, newCustomers: prevNewCustomers },      currentChart,      revenueChange: prevRev > 0 ? ((currRev - prevRev) / prevRev * 100).toFixed(1) : 0,      ordersChange: prevOrd > 0 ? ((currOrd - prevOrd) / prevOrd * 100).toFixed(1) : 0,      aovChange: prevAOV > 0 ? ((currAOV - prevAOV) / prevAOV * 100).toFixed(1) : 0,      customersChange: prevNewCustomers > 0 ? ((approxNewCustomers - prevNewCustomers) / prevNewCustomers * 100).toFixed(1) : 0,    };  }, [data]);  const statusData = useMemo(() => {    if (!data?.statusCounts) return [];    return Object.entries(data.statusCounts).map(([name, value]) => ({ name, value }));  }, [data]);  const exportCSV = () => {    const products = data?.productSales || [];    if (!products.length) return;    const headers = ["Product Name", "Units Sold", "Revenue (INR)"];    const rows = products.map(p => `"${p.name}",${p.totalSold},${p.revenue}`);    const csv = [headers.join(","), ...rows].join("\n");    const blob = new Blob([csv], { type: "text/csv" });    const url = URL.createObjectURL(blob);    const a = document.createElement("a");    a.href = url;    a.download = `product_sales_${new Date().toISOString().split('T')[0]}.csv`;    a.click();    URL.revokeObjectURL(url);  };  const renderChange = (change) => {    if (change === 0 || change === '0.0') return <span style={{ color: '#999', fontSize: '0.78rem' }}>â€”</span>;    const isUp = Number(change) > 0;    return (      <span style={{ color: isUp ? '#2e7d32' : '#c62828', fontWeight: 700, fontSize: '0.85rem' }}>        {isUp ? 'â–²' : 'â–¼'} {Math.abs(change)}%      </span>    );  };  if (loading && !data) {    return <div className="admin-loading"><div className="admin-spinner"></div> Loading analytics...</div>;  }  return (    <div>      <div className="admin-header">        <h1>Analytics & Reports</h1>        <div style={{ display: 'flex', gap: '0.5rem', alignItems: 'center' }}>          <div className="admin-filter-tabs">            {PERIOD_OPTIONS.map(opt => (              <button key={opt.value} className={`filter-tab ${period === opt.value ? 'active' : ''}`}                onClick={() => setPeriod(opt.value)}>                {opt.label}              </button>            ))}          </div>          <button className="admin-btn-outline admin-btn-sm" onClick={exportCSV}>            <Download size={14} /> Export CSV          </button>        </div>      </div>      {period === 'custom' && (        <div className="admin-toolbar" style={{ marginTop: '-1rem' }}>          <Calendar size={16} color="var(--text-secondary)" />          <input type="date" value={customStart} onChange={e => setCustomStart(e.target.value)}            style={{ padding: '0.4rem 0.6rem', borderRadius: '4px', border: '1px solid var(--border-color)', fontSize: '0.85rem', background: 'var(--bg-chocolate)', color: 'var(--text-warm-white)' }} />          <span style={{ color: 'var(--text-secondary)' }}>to</span>          <input type="date" value={customEnd} onChange={e => setCustomEnd(e.target.value)}            style={{ padding: '0.4rem 0.6rem', borderRadius: '4px', border: '1px solid var(--border-color)', fontSize: '0.85rem', background: 'var(--bg-chocolate)', color: 'var(--text-warm-white)' }} />        </div>      )}      <div className="stats-grid">        <div className="stat-card green">          <h3>Total Revenue</h3>          <p className="stat-value">{formatINR(stats.totalRevenue)}</p>          <p className="stat-sub">vs previous period {renderChange(revenueChange)}</p>        </div>        <div className="stat-card">          <h3>Total Orders</h3>          <p className="stat-value">{stats.totalOrders}</p>          <p className="stat-sub">vs previous period {renderChange(ordersChange)}</p>        </div>        <div className="stat-card blue">          <h3>Avg Order Value</h3>          <p className="stat-value">{formatINR(stats.avgOrderValue)}</p>          <p className="stat-sub">vs previous period {renderChange(aovChange)}</p>        </div>        <div className="stat-card gold">          <h3>New Customers</h3>          <p className="stat-value">{stats.newCustomers}</p>          <p className="stat-sub">vs previous period {renderChange(customersChange)}</p>        </div>      </div>      <div className="charts-grid">        <div className="admin-card">          <div className="admin-card-header">            <h2>Revenue Trend</h2>          </div>          <div style={{ width: '100%', height: 280 }}>            <ResponsiveContainer>              <LineChart data={currentChart}>                <CartesianGrid strokeDasharray="3 3" stroke="var(--border-color)" />                <XAxis dataKey="date" tick={{fontSize: 11, fill: 'var(--text-secondary)'}} tickFormatter={v => v.split('-').slice(1).join('/')} />                <YAxis tick={{fontSize: 11, fill: 'var(--text-secondary)'}} tickFormatter={v => v >= 1000 ? `${(v/1000).toFixed(0)}k` : v} />                <Tooltip formatter={v => [formatINR(v), "Revenue"]} labelFormatter={l => `Date: ${l}`} />                <Line type="monotone" dataKey="revenue" stroke="var(--accent-gold)" strokeWidth={2.5} dot={false} />              </LineChart>            </ResponsiveContainer>          </div>        </div>        <div className="admin-card">          <div className="admin-card-header">            <h2>Orders Trend</h2>          </div>          <div style={{ width: '100%', height: 280 }}>            <ResponsiveContainer>              <BarChart data={currentChart}>                <CartesianGrid strokeDasharray="3 3" stroke="var(--border-color)" />                <XAxis dataKey="date" tick={{fontSize: 11, fill: 'var(--text-secondary)'}} tickFormatter={v => v.split('-').slice(1).join('/')} />                <YAxis tick={{fontSize: 11, fill: 'var(--text-secondary)'}} allowDecimals={false} />                <Tooltip formatter={v => [v, "Orders"]} labelFormatter={l => `Date: ${l}`} />                <Bar dataKey="orders" fill="var(--accent-gold)" radius={[3, 3, 0, 0]} />              </BarChart>            </ResponsiveContainer>          </div>        </div>      </div>      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '1.5rem', marginBottom: '2rem' }}>        <div className="admin-card">          <div className="admin-card-header">            <h2>Top Selling Products</h2>          </div>          {data?.productSales?.length > 0 ? (            <table className="admin-table">              <thead>                <tr>                  <th>#</th>                  <th>Product</th>                  <th>Sold</th>                  <th>Revenue</th>                </tr>              </thead>              <tbody>                {data.productSales.slice(0, 10).map((p, i) => (                  <tr key={p.name}>                    <td style={{ width: 30, color: 'var(--text-secondary)' }}>{i + 1}</td>                    <td style={{ fontWeight: 600 }}>{p.name}</td>                    <td>{p.totalSold}</td>                    <td style={{ fontWeight: 700 }}>{formatINR(p.revenue)}</td>                  </tr>                ))}              </tbody>            </table>          ) : (            <div className="empty-state"><p>No product sales data for this period.</p></div>          )}        </div>        <div className="admin-card">          <div className="admin-card-header">            <h2>Orders by Status</h2>          </div>          {statusData.length > 0 ? (            <div style={{ display: 'flex', alignItems: 'center', height: 260 }}>              <ResponsiveContainer width="60%" height="100%">                <PieChart>                  <Pie data={statusData} cx="50%" cy="50%" innerRadius={50} outerRadius={90}                    dataKey="value" nameKey="name" paddingAngle={2}>                    {statusData.map((entry, i) => (                      <Cell key={entry.name} fill={STATUS_COLORS[entry.name] || '#999'} />                    ))}                  </Pie>                  <Tooltip formatter={v => [v, "Orders"]} />                </PieChart>              </ResponsiveContainer>              <div style={{ flex: 1 }}>                {statusData.map(entry => (                  <div key={entry.name} style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', marginBottom: '0.4rem', fontSize: '0.85rem' }}>                    <span style={{ width: 10, height: 10, borderRadius: '50%', background: STATUS_COLORS[entry.name] || '#999', display: 'inline-block' }}></span>                    <span style={{ flex: 1, textTransform: 'capitalize' }}>{entry.name}</span>                    <span style={{ fontWeight: 700 }}>{entry.value}</span>                  </div>                ))}              </div>            </div>          ) : (            <div className="empty-state"><p>No order data for this period.</p></div>          )}        </div>      </div>    </div>  );}