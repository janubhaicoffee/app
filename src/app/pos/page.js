"use client";import { useState, useEffect } from "react";import { useRouter } from "next/navigation";import { supabase } from "@/lib/supabase";import { fetchOutlets, prefetchOutletData } from "@/lib/offlineApi";import { Store, MapPin, Clock, ArrowRight } from "lucide-react";import "./pos.css";export default function PosOutletSelect() {  const router = useRouter();  const [outlets, setOutlets] = useState([]);  const [loading, setLoading] = useState(true);  const [error, setError] = useState(null);  useEffect(() => {    const load = async () => {      try {        const { data: { user } } = await supabase.auth.getUser();        if (!user) {          router.push("/auth/login");          return;        }        const result = await fetchOutlets(user.id);        if (result.error) throw new Error(result.error);        setOutlets(result.data || []);      } catch (err) {        setError(err.message);      } finally {        setLoading(false);      }    };    load();  }, [router]);  const handleSelect = (outlet) => {    sessionStorage.setItem("pos_outlet", JSON.stringify(outlet));    prefetchOutletData(outlet.id);    router.push("/pos/dashboard");  };  if (loading) {    return (      <div className="pos-fullscreen">        <div className="pos-loading">Loading outlets...</div>      </div>    );  }  if (error) {    return (      <div className="pos-fullscreen">        <div className="pos-error">{error}</div>      </div>    );  }  return (    <div className="pos-fullscreen">      <div style={{ textAlign: "center", padding: "40px 20px 20px" }}>        <Store size={40} style={{ color: "var(--text-warm-white)" }} />        <h1 style={{ fontSize: 24, marginTop: 12 }}>Janu Bhai Coffee POS</h1>        <p style={{ color: "var(--text-secondary)", fontSize: 14 }}>Select your outlet</p>      </div>      <div className="pos-outlet-grid">        {outlets.length === 0 ? (          <div className="pos-empty" style={{ gridColumn: "1/-1" }}>            No outlets assigned to you          </div>        ) : (          outlets.map((outlet) => (            <button              key={outlet.id}              className="pos-outlet-card"              onClick={() => handleSelect(outlet)}            >              <Store size={32} style={{ color: "var(--accent-gold)", marginBottom: 8 }} />              <h3>{outlet.name}</h3>              <p><MapPin size={12} /> {outlet.address || outlet.location || "No address"}</p>              <div style={{ marginTop: 12, display: "flex", alignItems: "center", justifyContent: "center", gap: 4, color: "var(--accent-gold)", fontSize: 13, fontWeight: 600 }}>                Enter POS <ArrowRight size={14} />              </div>            </button>          ))        )}      </div>    </div>  );}