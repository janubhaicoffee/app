"use client";import { useState, useEffect } from "react";import { useCart } from "@/context/CartContext";import { useRouter } from "next/navigation";import toast from "react-hot-toast";import "./page.css";const facts = [  "Did you know? Chikmagalur is the birthplace of coffee in India.",  "Our beans are shade-grown under a canopy of fruit trees.",  "Janu Bhai Coffee is 100% sourced directly from local farmers.",  "Freshly roasted in small batches to preserve maximum flavor.",  "Chicory adds that rich, dark, and perfectly bitter kick you love."];export default function CheckoutPage() {  const { cartItems, getCartTotal, clearCart } = useCart();  const router = useRouter();    const [formData, setFormData] = useState({    name: "",    email: "",    phone: "",    address: "",    city: "",    state: "",    pincode: "",    giftMessage: ""  });  const [userId, setUserId] = useState(null);  const [shippingRate, setShippingRate] = useState(null);  const [shippingLoading, setShippingLoading] = useState(false);  const [shippingError, setShippingError] = useState("");  const [paymentLoading, setPaymentLoading] = useState(false);    // Checkout Assistant State  const [assistantMessage, setAssistantMessage] = useState("Hi! I'm your Janu Bhai assistant. Need help with your address?");    const [checkoutMode, setCheckoutMode] = useState("standard");  const [subFrequency, setSubFrequency] = useState(null);  const [paymentMethod, setPaymentMethod] = useState("online");    const [factIndex, setFactIndex] = useState(0);  const [displayedFact, setDisplayedFact] = useState("");  const finalTotal = getCartTotal() + (shippingRate ? shippingRate.shipping_cost : 0);  // Typewriter Effect  useEffect(() => {    let currentText = facts[factIndex];    let i = 0;    let timeoutId;        setDisplayedFact("");        const typingInterval = setInterval(() => {      // Use substring to guarantee the exact text is rendered, avoiding async state batching issues      setDisplayedFact(currentText.substring(0, i));            if (i < currentText.length) {        i++;      } else {        clearInterval(typingInterval);        timeoutId = setTimeout(() => {          setFactIndex((prev) => (prev + 1) % facts.length);        }, 3000); // Wait 3s before next fact      }    }, 50); // Typing speed        return () => {      clearInterval(typingInterval);      if (timeoutId) clearTimeout(timeoutId);    };  }, [factIndex]);  useEffect(() => {    // Parse URL params    const searchParams = new URLSearchParams(window.location.search);    const mode = searchParams.get("mode");    const freq = searchParams.get("frequency");        // We defer the state updates to avoid React's synchronous setState in effect warning.    // Instead of doing it directly, we can do it asynchronously or just let it batch.    setTimeout(() => {      if (mode) setCheckoutMode(mode);      if (freq) setSubFrequency(freq);    }, 0);    // Check if user is logged in    import("@/lib/supabase").then(({ supabase }) => {      supabase.auth.getSession().then(({ data: { session } }) => {        if (session?.user) {          setUserId(session.user.id);          setFormData(prev => ({            ...prev,            email: session.user.email,            name: session.user.user_metadata?.full_name || prev.name          }));        }      });    });    // Load Razorpay script    const script = document.createElement("script");    script.src = "https://checkout.razorpay.com/v1/checkout.js";    script.async = true;    document.body.appendChild(script);  }, []);  useEffect(() => {    const fetchShippingRates = async (pincodeStr) => {      setShippingLoading(true);      setShippingError("");      try {        const weight = cartItems.reduce((acc, item) => acc + (500 * item.quantity), 0) || 500;        const res = await fetch('/api/shipping/rates', {          method: 'POST',          headers: { 'Content-Type': 'application/json' },          body: JSON.stringify({            destination: pincodeStr,            cartItems: cartItems          })        });        const data = await res.json();        if (data.success) {          setShippingRate(data);        } else {          // Non-blocking Serviceability          setAssistantMessage(`It looks like our regular couriers might have trouble delivering to ${pincodeStr}. If you have an alternate nearby address or work address, that might ensure a faster delivery!`);          setShippingRate({            shipping_cost: 60,            courier_name: "Standard Postal Delivery",            estimated_delivery_days: "5-7"          });        }      } catch (err) {        setAssistantMessage(`We're having trouble fetching live shipping rates for ${pincodeStr}, but don't worry, you can still place your order!`);        setShippingRate({          shipping_cost: 60,          courier_name: "Standard Postal Delivery",          estimated_delivery_days: "5-7"        });      } finally {        setShippingLoading(false);      }    };    const fetchCityState = async (pincodeStr) => {      try {        const res = await fetch(`https://api.postalpincode.in/pincode/${pincodeStr}`);        const data = await res.json();        if (data && data[0] && data[0].Status === "Success") {          const postOffice = data[0].PostOffice[0];          const fetchedCity = postOffice.District;          const fetchedState = postOffice.State;                    setFormData(prev => ({ ...prev, city: fetchedCity, state: fetchedState }));          setAssistantMessage(`Got it! Setting your location to ${fetchedCity}, ${fetchedState} based on your pincode.`);        }      } catch (err) {        console.error("Postal API error", err);      }    };    if (formData.pincode.length === 6) {      fetchCityState(formData.pincode);      fetchShippingRates(formData.pincode);    } else {      setTimeout(() => {        setShippingRate(null);        setShippingError("");      }, 0);    }  }, [formData.pincode, cartItems, getCartTotal]);  // Smart Landmark Assistance  useEffect(() => {    if (formData.address.length > 5 && formData.address.length < 15) {      const timeout = setTimeout(() => {        setAssistantMessage("Could you add a nearby landmark or colony name? It helps our delivery partners find your door much faster!");      }, 1500);      return () => clearTimeout(timeout);    }  }, [formData.address]);  const handleInputChange = (e) => {    setFormData({ ...formData, [e.target.name]: e.target.value });  };  const handlePayment = async (e) => {    e.preventDefault();    if (cartItems.length === 0) return;        setPaymentLoading(true);    try {      let rzpOrderId = null;      let subId = null;      const completeOrder = async (paymentId, orderId, signature) => {        const toastId = toast.loading("Confirming your order...");        try {          const res = await fetch('/api/order/complete', {            method: 'POST',            headers: { 'Content-Type': 'application/json' },            body: JSON.stringify({              formData,              cartItems,              shippingRate,              finalTotal,              paymentId,              razorpayOrderId: orderId,              razorpaySignature: signature,              userId,              isGift: checkoutMode === "gift",              giftMessage: formData.giftMessage,              isSubscription: checkoutMode === "subscription",              subscriptionId: subId,              subscriptionFrequency: subFrequency            })          });          const data = await res.json();          if (data.success) {            toast.success(`Order Placed Successfully!`, { id: toastId });            clearCart();            const orderNumber = data.order_number || data.orderId || '';            router.push(`/order-confirmation?order=${encodeURIComponent(orderNumber)}`);          } else {            toast.error("Order completion failed: " + data.error, { id: toastId });            setPaymentLoading(false);          }        } catch (e) {          console.error(e);          toast.error("Error finalizing order. Please contact support.", { id: toastId });          setPaymentLoading(false);        }      };      if (paymentMethod === "cod") {        await completeOrder(`COD_${Date.now()}`, `ORDER_${Date.now()}`, "COD_SIGNATURE");        return;      }      if (checkoutMode === "subscription") {        const res = await fetch('/api/razorpay/subscription', {          method: 'POST',          headers: { 'Content-Type': 'application/json' },          body: JSON.stringify({ amount: finalTotal, frequency: subFrequency })        });        const data = await res.json();        if (!data.subscriptionId) {          toast.error("Failed to setup subscription: " + (data.error || "Unknown error"));          setPaymentLoading(false);          return;        }        subId = data.subscriptionId;      } else {        const res = await fetch('/api/razorpay', {          method: 'POST',          headers: { 'Content-Type': 'application/json' },          body: JSON.stringify({             cartItems: cartItems,             shippingCost: shippingRate ? shippingRate.shipping_cost : 0,            formData: formData,            userId: userId          })        });        const data = await res.json();        if (!data.orderId) {          toast.error("Failed to initialize payment: " + (data.error || "Unknown error"));          setPaymentLoading(false);          return;        }        rzpOrderId = data.orderId;      }      const keyId = process.env.NEXT_PUBLIC_RAZORPAY_KEY_ID;      if (!keyId) {        toast.error("Razorpay Configuration Missing. Please contact support.");        setPaymentLoading(false);        return;      }      const options = {        key: keyId,        amount: checkoutMode === "subscription" ? undefined : finalTotal * 100, // subscriptions dont pass amount here        currency: "INR",        name: "Janu Bhai Coffee",        description: checkoutMode === "subscription" ? `Coffee Subscription (${subFrequency})` : "Order Checkout",        order_id: rzpOrderId || undefined,        subscription_id: subId || undefined,        handler: async function (response) {          await completeOrder(            response.razorpay_payment_id,            checkoutMode === "subscription" ? response.razorpay_subscription_id : response.razorpay_order_id,            response.razorpay_signature          );        },        modal: {          ondismiss: function() {            setPaymentLoading(false);          }        },        prefill: {          name: formData.name,          email: formData.email,          contact: formData.phone        },        theme: {          color: "#B71C1C"        }      };      const rzp = new window.Razorpay(options);      rzp.on('payment.failed', function (response){        toast.error(`Payment Failed: ${response.error.description}`);        setPaymentLoading(false);      });      rzp.open();    } catch (error) {      console.error(error);      toast.error("Payment system failed to initialize. Please try again.");      setPaymentLoading(false);    }  };  if (cartItems.length === 0) {    return (      <main className="checkout-page text-center">        <div className="container">          <h2>Your cart is empty. Please add items before checking out.</h2>          <button className="btn-primary" onClick={() => router.push('/')} style={{marginTop: '20px'}}>Go Back</button>        </div>      </main>    );  }  return (    <main className="checkout-page">      <div className="container" style={{ maxWidth: '1200px' }}>                <div className="checkout-master-card">          <div className="checkout-form-section">            <div className="checkout-assistant">              <div className="assistant-avatar">JB</div>              <p className="assistant-message">{assistantMessage}</p>            </div>                        <h2 className="section-label">{checkoutMode === "gift" ? "Recipient's Shipping Details" : "Shipping Details"}</h2>            <form onSubmit={handlePayment} className="checkout-form">              <div className="form-group">                <label htmlFor="name">{checkoutMode === "gift" ? "Recipient's Full Name" : "Full Name"}</label>                <input id="name" type="text" name="name" required value={formData.name} onChange={handleInputChange} placeholder="e.g. Rahul Sharma" />              </div>              <div className="form-row">                <div className="form-group">                  <label htmlFor="email">Email</label>                  <input id="email" type="email" name="email" required value={formData.email} onChange={handleInputChange} placeholder="rahul@example.com" />                </div>                <div className="form-group">                  <label htmlFor="phone">Phone Number</label>                  <input id="phone" type="tel" name="phone" required value={formData.phone} onChange={handleInputChange} placeholder="+91 9876543210" />                </div>              </div>              <div className="form-group">                <label htmlFor="address">Address</label>                <textarea id="address" name="address" rows="3" required value={formData.address} onChange={handleInputChange} placeholder="House/Flat No., Street, Landmark"></textarea>              </div>              {checkoutMode === "gift" && (                <div className="form-group">                  <label htmlFor="giftMessage">Gift Message (Optional)</label>                  <textarea id="giftMessage" name="giftMessage" rows="2" value={formData.giftMessage} onChange={handleInputChange} placeholder="Write a nice message..."></textarea>                </div>              )}              <div className="form-row">                <div className="form-group">                  <label htmlFor="city">City</label>                  <input id="city" type="text" name="city" required value={formData.city} onChange={handleInputChange} placeholder="New Delhi" />                </div>                <div className="form-group">                  <label htmlFor="state">State</label>                  <input id="state" type="text" name="state" required value={formData.state} onChange={handleInputChange} placeholder="Delhi" />                </div>                <div className="form-group">                  <label htmlFor="pincode">PIN Code</label>                  <input id="pincode" type="text" name="pincode" required value={formData.pincode} onChange={handleInputChange} placeholder="110025" />                </div>              </div>                            {shippingLoading && <p className="shipping-status calculating">Calculating shipping rates...</p>}              {shippingError && <p className="shipping-status error">{shippingError}</p>}              {shippingRate && (                <div className="shipping-status success">                  <p><strong>Delivery via {shippingRate.courier_name}</strong></p>                  <p className="shipping-days">Estimated: {shippingRate.estimated_delivery_days} Days</p>                </div>              )}              <div className="payment-method-section mt-20 mb-20" style={{marginTop: '20px', marginBottom: '20px'}}>                <label className="section-label" style={{display: 'block', marginBottom: '10px'}}>Payment Method</label>                <div className="payment-methods-grid" style={{display: 'flex', gap: '15px'}}>                  <label className={`payment-option ${paymentMethod === 'online' ? 'active' : ''}`} style={{flex: 1, padding: '12px', border: paymentMethod === 'online' ? '2px solid var(--primary-color)' : '1px solid #ccc', borderRadius: '8px', cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '8px'}}>                    <input type="radio" name="paymentMethod" value="online" checked={paymentMethod === "online"} onChange={(e) => setPaymentMethod(e.target.value)} style={{accentColor: 'var(--primary-color)'}} />                    <span>Pay Online</span>                  </label>                  {checkoutMode !== "subscription" && (                    <label className={`payment-option ${paymentMethod === 'cod' ? 'active' : ''}`} style={{flex: 1, padding: '12px', border: paymentMethod === 'cod' ? '2px solid var(--primary-color)' : '1px solid #ccc', borderRadius: '8px', cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '8px'}}>                      <input type="radio" name="paymentMethod" value="cod" checked={paymentMethod === "cod"} onChange={(e) => setPaymentMethod(e.target.value)} style={{accentColor: 'var(--primary-color)'}} />                      <span>Cash on Delivery</span>                    </label>                  )}                </div>              </div>              <button type="submit" className="btn-primary full-width mt-20 pulse-hover" disabled={shippingLoading || !!shippingError || formData.pincode.length !== 6 || paymentLoading}>                {paymentLoading ? 'PROCESSING...' : `PAY â‚¹ ${finalTotal}`}              </button>            </form>          </div>          <div className="checkout-summary-section">            <h2 className="section-label">Order Summary</h2>            <div className="summary-items">              {cartItems.map(item => (                <div key={item.id} className="summary-item">                  <span className="summary-qty">{item.quantity}x</span>                  <span className="summary-name">{item.name}</span>                  <span className="summary-price">â‚¹ {item.price * item.quantity}</span>                </div>              ))}            </div>                        <div className="summary-divider"></div>                        <div className="summary-row">              <span>Subtotal</span>              <span>â‚¹ {getCartTotal()}</span>            </div>                        {shippingRate && (              <div className="summary-row">                <span>Shipping</span>                <span>â‚¹ {shippingRate.shipping_cost}</span>              </div>            )}                        <div className="summary-divider thick"></div>                        <div className="summary-row total">              <span>Total to Pay</span>              <span>â‚¹ {finalTotal}</span>            </div>          </div>        </div>        {/* Animated Coffee Facts (Typewriter) */}        <div className="checkout-facts">          <div className="typewriter-container">            <span className="fact-label">Coffee Fact: </span>            <span className="typewriter-text">{displayedFact}<span className="cursor">|</span></span>          </div>        </div>      </div>    </main>  );}