'use client';import { useEffect } from 'react';export function reportWebVitals(metric) {  // Analytics callback  if (process.env.NODE_ENV === 'production') {    const body = JSON.stringify(metric);    const url = '/api/analytics';    // Use sendBeacon for reliability during page unload    if (navigator.sendBeacon) {      navigator.sendBeacon(url, body);    } else {      fetch(url, { body, method: 'POST', keepalive: true });    }  }}export function WebVitals() {  useEffect(() => {    const { onCLS, onFID, onLCP, onFCP, onTTFB, onINP } = require('web-vitals');    onCLS(reportWebVitals);    onFID(reportWebVitals);    onLCP(reportWebVitals);    onFCP(reportWebVitals);    onTTFB(reportWebVitals);    onINP(reportWebVitals);  }, []);  return null;}