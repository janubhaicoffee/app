"use client";import { useState, useRef, useEffect } from "react";import { Calendar, ChevronDown } from "lucide-react";const presets = [  { label: "Today", getRange: () => { const d = new Date(); return { start: d, end: d }; } },  { label: "Yesterday", getRange: () => {    const d = new Date(); d.setDate(d.getDate() - 1);    return { start: d, end: d };  }},  { label: "Last 7 Days", getRange: () => {    const end = new Date(); const start = new Date(); start.setDate(start.getDate() - 6);    return { start, end };  }},  { label: "Last 30 Days", getRange: () => {    const end = new Date(); const start = new Date(); start.setDate(start.getDate() - 29);    return { start, end };  }},  { label: "This Month", getRange: () => {    const now = new Date();    return { start: new Date(now.getFullYear(), now.getMonth(), 1), end: now };  }},  { label: "Last Month", getRange: () => {    const now = new Date();    const start = new Date(now.getFullYear(), now.getMonth() - 1, 1);    const end = new Date(now.getFullYear(), now.getMonth(), 0);    return { start, end };  }},  { label: "Custom Range", getRange: () => null },];function formatDate(d) {  return d.toISOString().split("T")[0];}export default function DatePicker({ value, onChange, className = "" }) {  const [isOpen, setIsOpen] = useState(false);  const [activePreset, setActivePreset] = useState(null);  const [customStart, setCustomStart] = useState("");  const [customEnd, setCustomEnd] = useState("");  const ref = useRef(null);  useEffect(() => {    function handleClickOutside(e) {      if (ref.current && !ref.current.contains(e.target)) setIsOpen(false);    }    if (isOpen) document.addEventListener("mousedown", handleClickOutside);    return () => document.removeEventListener("mousedown", handleClickOutside);  }, [isOpen]);  function handlePreset(preset) {    setActivePreset(preset.label);    if (preset.label === "Custom Range") {      setIsOpen(true);      return;    }    const range = preset.getRange();    onChange({ preset: preset.label, ...range });    setIsOpen(false);  }  function handleCustomApply() {    if (customStart && customEnd) {      onChange({ preset: "Custom Range", start: new Date(customStart), end: new Date(customEnd) });      setIsOpen(false);    }  }  const displayLabel = value?.preset    ? value.preset === "Custom Range"      ? `${value.start ? formatDate(value.start) : "?"} â€“ ${value.end ? formatDate(value.end) : "?"}`      : value.preset    : "Select Date Range";  return (    <div ref={ref} className={`relative ${className}`}>      <button        onClick={() => setIsOpen(!isOpen)}        className="inline-flex items-center gap-2 px-3.5 py-2 bg-[var(--bg-chocolate)] border border-[var(--border-color)] rounded-lg text-sm font-medium text-[var(--text-warm-white)] hover:border-[var(--accent-gold)] transition-colors"      >        <Calendar size={16} className="text-[var(--text-secondary)]" />        <span className="min-w-[120px] text-left">{displayLabel}</span>        <ChevronDown size={14} className={`text-[var(--text-secondary)] transition-transform ${isOpen ? "rotate-180" : ""}`} />      </button>      {isOpen && (        <div className="absolute z-50 mt-1.5 left-0 bg-[var(--bg-chocolate)] border border-[var(--border-color)] rounded-lg shadow-lg p-2 min-w-[220px]">          <div className="flex flex-col gap-0.5">            {presets.map((preset) => (              <button                key={preset.label}                onClick={() => handlePreset(preset)}                className={`w-full text-left px-3 py-2 text-sm rounded-md transition-colors ${                  activePreset === preset.label                    ? "bg-[var(--accent-gold)] text-[var(--bg-espresso)] font-bold"                    : "text-[var(--text-warm-white)] hover:bg-[var(--accent-gold-light)] font-medium"                }`}              >                {preset.label}              </button>            ))}          </div>          {activePreset === "Custom Range" && (            <div className="mt-2 pt-2 border-t border-[var(--border-color)]">              <div className="flex flex-col gap-2">                <input                  type="date"                  value={customStart}                  onChange={(e) => setCustomStart(e.target.value)}                  className="w-full px-2.5 py-1.5 text-sm border border-[var(--border-color)] rounded-md outline-none focus:border-[var(--accent-gold)] bg-[var(--bg-espresso)] text-[var(--text-warm-white)]"                />                <input                  type="date"                  value={customEnd}                  onChange={(e) => setCustomEnd(e.target.value)}                  className="w-full px-2.5 py-1.5 text-sm border border-[var(--border-color)] rounded-md outline-none focus:border-[var(--accent-gold)] bg-[var(--bg-espresso)] text-[var(--text-warm-white)]"                />                <button                  onClick={handleCustomApply}                  disabled={!customStart || !customEnd}                  className="w-full px-3 py-1.5 text-sm font-bold bg-[var(--accent-gold)] text-[var(--bg-espresso)] rounded-md hover:brightness-110 disabled:opacity-50 disabled:cursor-not-allowed"                >                  Apply                </button>              </div>            </div>          )}        </div>      )}    </div>  );}