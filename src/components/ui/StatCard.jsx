"use client";import { TrendingUp, TrendingDown, Minus } from "lucide-react";const colorMap = {  green: "border-t-emerald-500",  red: "border-t-[var(--accent-gold)]",  gold: "border-t-[var(--accent-gold)]",  blue: "border-t-blue-500",  default: "border-t-[var(--accent-gold)]",};const trendIcons = {  up: TrendingUp,  down: TrendingDown,  flat: Minus,};const trendColors = {  up: "text-emerald-400",  down: "text-red-400",  flat: "text-[var(--text-secondary)]",};export default function StatCard({  icon: Icon,  label,  value,  trend = "flat",  trendValue,  color = "default",  className = "",}) {  const TrendIcon = trendIcons[trend] || Minus;  const topBorder = colorMap[color] || colorMap.default;  return (    <div      className={`bg-[var(--bg-chocolate)] rounded-xl border border-[var(--border-color)] shadow-[0_1px_4px_rgba(0,0,0,0.3)] p-5 border-t-3 ${topBorder} transition-transform hover:-translate-y-0.5 ${className}`}    >      <div className="flex items-start justify-between mb-2">        <h3 className="m-0 text-xs font-bold text-[var(--text-secondary)] uppercase tracking-wide">          {label}        </h3>        {Icon && (          <div className="text-[var(--text-secondary)] flex-shrink-0 ml-2">            <Icon size={18} />          </div>        )}      </div>      <p className="m-0 text-2xl font-bold text-[var(--text-warm-white)]">{value}</p>      {trendValue && (        <div className={`flex items-center gap-1 mt-1.5 text-xs font-semibold ${trendColors[trend] || trendColors.flat}`}>          <TrendIcon size={14} />          <span>{trendValue}</span>        </div>      )}    </div>  );}