"use client";const variantStyles = {  underline:    "border-b-2 border-transparent -mb-px text-[var(--text-secondary)] hover:text-[var(--accent-gold)] hover:border-[var(--accent-gold)]",  pills:    "rounded-lg text-[var(--text-secondary)] hover:bg-[var(--accent-gold-light)] hover:text-[var(--accent-gold)]",};const activeVariantStyles = {  underline: "text-[var(--accent-gold)] border-[var(--accent-gold)]",  pills: "bg-[var(--accent-gold)] text-[var(--bg-espresso)] hover:bg-[var(--accent-gold)] hover:text-[var(--bg-espresso)] shadow-sm font-bold",};export default function Tabs({ tabs, activeTab, onChange, variant = "underline", className = "" }) {  return (    <div      className={`flex gap-0 ${variant === "underline" ? "border-b border-[var(--border-color)]" : "gap-1"} ${className}`}    >      {tabs.map((tab) => {        const isActive = activeTab === tab.id;        const Icon = tab.icon;        return (          <button            key={tab.id}            onClick={() => onChange(tab.id)}            className={`inline-flex items-center gap-2 px-4 py-2.5 text-sm font-semibold transition-all duration-200 cursor-pointer whitespace-nowrap ${variantStyles[variant]} ${isActive ? activeVariantStyles[variant] : ""}`}          >            {Icon && <Icon size={16} />}            {tab.label}          </button>        );      })}    </div>  );}