"use client";import { useState, useMemo } from "react";import { ChevronUp, ChevronDown, ChevronsUpDown, Search, X } from "lucide-react";import Pagination from "./Pagination";export default function DataGrid({  columns = [],  data = [],  pageSize = 20,  loading = false,  onRowClick,  className = "",}) {  const [sortKey, setSortKey] = useState(null);  const [sortDir, setSortDir] = useState("asc");  const [page, setPage] = useState(1);  const [filters, setFilters] = useState({});  const handleSort = (key) => {    if (sortKey === key) {      setSortDir((d) => (d === "asc" ? "desc" : "asc"));    } else {      setSortKey(key);      setSortDir("asc");    }    setPage(1);  };  const filteredData = useMemo(() => {    let result = data;    Object.entries(filters).forEach(([key, value]) => {      if (!value) return;      result = result.filter((row) => {        const cellVal = String(row[key] ?? "").toLowerCase();        return cellVal.includes(value.toLowerCase());      });    });    return result;  }, [data, filters]);  const sortedData = useMemo(() => {    if (!sortKey) return filteredData;    return [...filteredData].sort((a, b) => {      const aVal = a[sortKey];      const bVal = b[sortKey];      if (aVal == null) return 1;      if (bVal == null) return -1;      if (typeof aVal === "string") {        return sortDir === "asc" ? aVal.localeCompare(bVal) : bVal.localeCompare(aVal);      }      return sortDir === "asc" ? aVal - bVal : bVal - aVal;    });  }, [filteredData, sortKey, sortDir]);  const totalPages = Math.max(1, Math.ceil(sortedData.length / pageSize));  const paginatedData = sortedData.slice((page - 1) * pageSize, page * pageSize);  if (loading) {    return (      <div className="flex items-center justify-center py-16 text-[var(--text-secondary)]">        <div className="w-7 h-7 border-2 border-[var(--border-color)] border-t-[var(--accent-gold)] rounded-full animate-spin mr-3" />        Loading...      </div>    );  }  return (    <div className={className}>      <div className="overflow-x-auto rounded-lg border border-[var(--border-color)]">        <table className="w-full border-collapse">          <thead>            <tr className="bg-[var(--bg-espresso)]">              {columns.map((col) => (                <th                  key={col.key}                  className="px-4 py-3 text-left text-xs font-bold text-[var(--accent-gold)] uppercase tracking-wider"                  style={col.width ? { width: col.width } : undefined}                >                  <div                    className="inline-flex items-center gap-1.5 cursor-pointer select-none hover:text-[var(--accent-gold)]"                    onClick={() => handleSort(col.key)}                  >                    {col.label}                    {sortKey === col.key ? (                      sortDir === "asc" ? (                        <ChevronUp size={14} />                      ) : (                        <ChevronDown size={14} />                      )                    ) : (                      <ChevronsUpDown size={14} className="text-[var(--text-secondary)]/40" />                    )}                  </div>                  {col.filter && (                    <div className="mt-1.5 relative" onClick={(e) => e.stopPropagation()}>                      <Search size={12} className="absolute left-2 top-1/2 -translate-y-1/2 text-[var(--text-secondary)]" />                      <input                        type="text"                        placeholder="Filter..."                        value={filters[col.key] || ""}                        onChange={(e) => {                          setFilters((f) => ({ ...f, [col.key]: e.target.value }));                          setPage(1);                        }}                        className="w-full pl-7 pr-6 py-1 text-[0.7rem] border border-[var(--border-color)] rounded outline-none focus:border-[var(--accent-gold)] bg-[var(--bg-chocolate)] text-[var(--text-warm-white)] placeholder-[var(--text-secondary)]"                      />                      {filters[col.key] && (                        <button                          onClick={() => {                            setFilters((f) => ({ ...f, [col.key]: "" }));                            setPage(1);                          }}                          className="absolute right-1.5 top-1/2 -translate-y-1/2 text-[var(--text-secondary)] hover:text-[var(--text-warm-white)]"                        >                          <X size={12} />                        </button>                      )}                    </div>                  )}                </th>              ))}            </tr>          </thead>          <tbody>            {paginatedData.length === 0 ? (              <tr>                <td colSpan={columns.length} className="px-4 py-12 text-center text-sm text-[var(--text-secondary)]">                  No data found.                </td>              </tr>            ) : (              paginatedData.map((row, i) => (                <tr                  key={row.id || i}                  onClick={() => onRowClick?.(row)}                  className={`border-t border-[var(--border-color)] transition-colors ${                    onRowClick ? "cursor-pointer hover:bg-[var(--accent-gold-light)]" : "hover:bg-[var(--accent-gold-light)]"                  }`}                >                  {columns.map((col) => (                    <td key={col.key} className="px-4 py-3 text-sm text-[var(--text-warm-white)]">                      {col.render ? col.render(row[col.key], row) : row[col.key]}                    </td>                  ))}                </tr>              ))            )}          </tbody>        </table>      </div>      {totalPages > 1 && (        <div className="mt-4 flex items-center justify-between">          <span className="text-xs text-[var(--text-secondary)]">            {sortedData.length} result{sortedData.length !== 1 ? "s" : ""}          </span>          <Pagination page={page} totalPages={totalPages} onPageChange={(p) => setPage(p)} />        </div>      )}    </div>  );}