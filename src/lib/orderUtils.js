import { createShipment } from "./nimbuspost";import { supabaseAdmin } from "./supabaseAdmin";import { Resend } from "resend";const resend = new Resend(process.env.RESEND_API_KEY || "re_dummy");export async function finalizeOrder(orderRow, paymentId) {  try {    // 1. Fetch current status to avoid race conditions    const { data: currentOrder, error: fetchErr } = await supabaseAdmin      .from("orders")      .select("status, awb_number, customer_email, customer_phone, total_amount, order_number, user_id, shipping_address")      .eq("id", orderRow.id)      .single();    if (fetchErr || !currentOrder) {      throw new Error(`Order not found or fetch failed: ${fetchErr?.message}`);    }    if (currentOrder.status !== "pending") {      console.log(`Order ${orderRow.id} is already processed with status: ${currentOrder.status}`);      return { success: true, awb: currentOrder.awb_number, alreadyProcessed: true };    }    // 2. Fetch order items    const { data: orderItems, error: itemsError } = await supabaseAdmin      .from("order_items")      .select("*")      .eq("order_id", orderRow.id);    if (itemsError || !orderItems || orderItems.length === 0) {      throw new Error(`Failed to load order items: ${itemsError?.message}`);    }    const shippingAddress = typeof currentOrder.shipping_address === "string"       ? JSON.parse(currentOrder.shipping_address)       : currentOrder.shipping_address || {};    // Calculate shipping charges (total_amount - order items price sum)    const itemsTotal = orderItems.reduce((sum, item) => sum + (Number(item.price) * item.quantity), 0);    const shippingCharges = Math.max(0, Number(currentOrder.total_amount) - itemsTotal);    const isCOD = paymentId && paymentId.startsWith("COD_");    let awbNumber = null;    let shipmentData = null;    // 3. Create Nimbuspost Shipment    if (orderItems.length > 0) {      // Fetch product weight (mock/fallback to 500g if not found)      let totalWeight = 0;      for (const item of orderItems) {        const { data: p } = await supabaseAdmin.from("products").select("weight").eq("id", item.product_id).maybeSingle();        totalWeight += (p?.weight || 500) * item.quantity;      }      const orderData = {        order_number: currentOrder.order_number,        shipping_charges: shippingCharges,        discount: 0,        cod_charges: isCOD ? Number(currentOrder.total_amount) : 0,        payment_type: isCOD ? "cod" : "prepaid",        order_amount: Number(currentOrder.total_amount),        package_weight: totalWeight,        package_length: 10,        package_breadth: 10,        package_height: 10,        request_auto_pickup: "yes",        consignee: {          name: shippingAddress.name || "Customer",          address: shippingAddress.address || "Address details",          address_2: "",          city: shippingAddress.city || "",          state: shippingAddress.state || "",          pincode: shippingAddress.pincode || "",          phone: currentOrder.customer_phone || "9999999999"        },        pickup: {          warehouse_name: "Janu Bhai HQ",          name: "Janu Bhai Team",          address: "Janu Bhai Fulfillment Center",          address_2: "",          city: "New Delhi",          state: "Delhi",          pincode: process.env.NIMBUSPOST_WAREHOUSE_PINCODE || "110001",          phone: "9999999999"        },        order_items: orderItems.map(item => ({          name: item.product_name,          qty: item.quantity.toString(),          price: item.price.toString(),          sku: item.product_id        })),        courier_id: "1" // Default standard courier      };      try {        shipmentData = await createShipment(orderData);        awbNumber = shipmentData?.awb_number;      } catch (shipmentErr) {        console.error("Failed to create Nimbuspost shipment:", shipmentErr);      }    }    const nextStatus = awbNumber ? "processing" : "payment_successful_shipping_failed";    // 4. Update order record in Supabase    const { error: updateErr } = await supabaseAdmin      .from("orders")      .update({        status: nextStatus,        awb_number: awbNumber,        razorpay_payment_id: paymentId,        updated_at: new Date().toISOString()      })      .eq("id", orderRow.id);    if (updateErr) {      throw new Error(`Failed to update order status in DB: ${updateErr.message}`);    }    // 5. Deduct Stock    for (const item of orderItems) {      try {        const { data: p } = await supabaseAdmin.from("products").select("*").eq("id", item.product_id).single();        if (p) {          if (item.variant_id && p.variants && Array.isArray(p.variants)) {            const newVariants = p.variants.map(v => {              if (v.id === item.variant_id) {                return { ...v, stock: Math.max(0, v.stock - item.quantity) };              }              return v;            });            await supabaseAdmin.from("products").update({ variants: newVariants }).eq("id", p.id);          } else {            await supabaseAdmin.from("products").update({ stock: Math.max(0, p.stock - item.quantity) }).eq("id", p.id);          }        }      } catch (stockErr) {        console.error(`Failed to deduct stock for product ${item.product_id}:`, stockErr);      }    }    // 6. Award Loyalty Points    try {      let pointsUser = currentOrder.user_id;      if (!pointsUser && currentOrder.customer_email) {        const { data: matchedProfile } = await supabaseAdmin          .from("profiles")          .select("id")          .eq("email", currentOrder.customer_email)          .maybeSingle();        if (matchedProfile) {          pointsUser = matchedProfile.id;        }      }      if (pointsUser) {        const pointsToAward = Math.floor(Number(currentOrder.total_amount) / 10);        if (pointsToAward > 0) {          const { data: profile } = await supabaseAdmin            .from("user_profiles")            .select("total_points")            .eq("id", pointsUser)            .maybeSingle();          const currentPoints = profile?.total_points || 0;          const newPoints = currentPoints + pointsToAward;          if (profile) {            await supabaseAdmin              .from("user_profiles")              .update({ total_points: newPoints })              .eq("id", pointsUser);          } else {            await supabaseAdmin              .from("user_profiles")              .insert({ id: pointsUser, total_points: newPoints + 15 });          }          await supabaseAdmin            .from("points_ledger")            .insert({              user_id: pointsUser,              points_awarded: pointsToAward,              action_type: `Order Purchase #${currentOrder.order_number}`            });        }      }    } catch (pointsErr) {      console.error("Failed to award loyalty points:", pointsErr);    }    // 7. Send Email Confirmation    if (currentOrder.customer_email && process.env.RESEND_API_KEY) {      try {        await resend.emails.send({          from: "Janu Bhai Coffee <hello@janubhai.com>",          to: [currentOrder.customer_email],          subject: `Order Confirmation #${currentOrder.order_number} - Janu Bhai`,          html: `            <div style="font-family: sans-serif; max-width: 600px; margin: 0 auto; color: #333;">              <h1 style="color: #e74c3c;">Thank you for your order!</h1>              <p>Hi ${shippingAddress.name || "Coffee Lover"},</p>              <p>We've received your order <strong>#${currentOrder.order_number}</strong> and we're getting it ready to ship.</p>                            <div style="background: #f9f9f9; padding: 15px; border-radius: 5px; margin: 20px 0;">                <h3 style="margin-top: 0;">Order Details:</h3>                <p><strong>Total Amount:</strong> â‚¹${currentOrder.total_amount}</p>                ${awbNumber ? `<p><strong>Tracking Number (AWB):</strong> ${awbNumber}</p>` : ""}              </div>              <h3>Shipping Address:</h3>              <p>${shippingAddress.address || ""}<br/>${shippingAddress.city || ""}, ${shippingAddress.pincode || ""}</p>              <p style="margin-top: 30px;">You can track your order status anytime on our website tracking page using your Order Number.</p>              <p>Cheers,<br/><strong>The Janu Bhai Team</strong></p>            </div>          `        });      } catch (emailErr) {        console.error("Failed to send confirmation email:", emailErr);      }    }    return { success: true, awb: awbNumber };  } catch (error) {    console.error(`Error in finalizeOrder:`, error);    throw error;  }}