// Offline queue for failed API requestsconst STORAGE_KEY = 'offline_queue';class OfflineQueue {  constructor() {    this.queue = this.load();  }  load() {    try {      const data = localStorage.getItem(STORAGE_KEY);      return data ? JSON.parse(data) : [];    } catch {      return [];    }  }  save() {    try {      localStorage.setItem(STORAGE_KEY, JSON.stringify(this.queue));    } catch {      // Storage full - clear oldest items      this.queue = this.queue.slice(-10);      this.save();    }  }  async enqueue(request) {    const entry = {      id: Date.now().toString(36) + Math.random().toString(36).slice(2, 7),      url: request.url,      method: request.method,      headers: Object.fromEntries(request.headers.entries()),      body: await request.clone().text(),      timestamp: Date.now(),    };    this.queue.push(entry);    this.save();    return entry;  }  async process() {    if (this.queue.length === 0) return;    const batch = [...this.queue];    this.queue = [];    this.save();    for (const entry of batch) {      try {        const response = await fetch(entry.url, {          method: entry.method,          headers: entry.headers,          body: entry.method !== 'GET' ? entry.body : undefined,        });        if (!response.ok) {          throw new Error(`HTTP ${response.status}`);        }      } catch {        // Re-enqueue failed entries        this.queue.push(entry);        this.save();      }    }  }}export const offlineQueue = new OfflineQueue();// Auto-process queue when onlineif (typeof window !== 'undefined') {  window.addEventListener('online', () => offlineQueue.process());}