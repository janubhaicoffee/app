"use server";import { supabaseAdmin } from "@/lib/supabaseAdmin";// Secure token lookup and assertionexport async function verifyGiftToken(token) {  if (!token) return { error: "No claim token provided" };  try {    const { data: tx, error } = await supabaseAdmin      .from("gift_transactions")      .select("*, products (name, description, image_url)")      .eq("claim_token", token)      .single();    if (error || !tx) {      return { error: "Legitimacy assertion failed: Invalid or expired claim token." };    }    return { success: true, transaction: tx };  } catch (err) {    console.error("verifyGiftToken error:", err);    return { error: "Server authentication failure" };  }}// Complete fulfillment route update and order generationexport async function claimGift(token, shippingDetails) {  if (!token) return { error: "No claim token provided" };    const { fullName, address, city, pincode, phone } = shippingDetails;  if (!fullName || !address || !city || !pincode || !phone) {    return { error: "All recipient shipping address fields must be filled" };  }  try {    // 1. Double check current transaction status securely    const { data: tx, error: fetchErr } = await supabaseAdmin      .from("gift_transactions")      .select("status, product_id")      .eq("claim_token", token)      .single();    if (fetchErr || !tx) {      return { error: "Fulfillment target resolution failed" };    }    if (tx.status !== "pending") {      return { error: "Fulfillment has already been claimed and processed" };    }    // 2. Commit shipping details and update status to claimed    const { error: updateErr } = await supabaseAdmin      .from("gift_transactions")      .update({        status: "claimed",        shipping_address: {          full_name: fullName,          address: address,          city: city,          pincode: pincode,          phone: phone,          claimed_at: new Date().toISOString()        }      })      .eq("claim_token", token);    if (updateErr) {      throw updateErr;    }    // 3. Programmatically inject order tracking entry in checkout/fulfillment tables    const { data: order, error: orderErr } = await supabaseAdmin      .from("orders")      .insert({        total_amount: 0, // prepaid        status: "pending",        is_gift: true,        gift_message: `Gift Claimed via Token: ${token}`,        customer_phone: phone,        created_at: new Date()      })      .select()      .single();    if (orderErr) {      console.error("Logistical order backfill failed:", orderErr);    } else {      // Get product details      const { data: prod } = await supabaseAdmin        .from("products")        .select("name, price")        .eq("id", tx.product_id)        .single();      await supabaseAdmin        .from("order_items")        .insert({          order_id: order.id,          product_id: tx.product_id,          product_name: prod ? `Gift: ${prod.name}` : `Gift Product (${tx.product_id})`,          quantity: 1,          price: 0        });    }    return { success: true };  } catch (err) {    console.error("claimGift error:", err);    return { error: "Fulfillment routing error occurred" };  }}