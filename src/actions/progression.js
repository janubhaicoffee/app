"use server";import { createClient } from "@/lib/supabaseWrapper";const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;async function getAuthenticatedClient(accessToken) {  return createClient(supabaseUrl, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY, {    auth: {      persistSession: false,      autoRefreshToken: false    },    global: {      headers: {        Authorization: `Bearer ${accessToken}`      }    }  });}export async function getUserProgression(accessToken) {  if (!accessToken) {    return { error: "No access token provided" };  }  try {    const client = await getAuthenticatedClient(accessToken);    const { data: { user }, error: authError } = await client.auth.getUser();        if (authError || !user) {      return { error: "Invalid session or unauthorized" };    }    // Fetch user profile points    let { data: profile, error: profileError } = await client      .from("user_profiles")      .select("*")      .single();    // If profile doesn't exist, backfill    if (profileError && (profileError.code === "PGRST116" || profileError.message?.includes("0 rows"))) {      const { data: newProfile, error: createError } = await client        .from("user_profiles")        .insert({ id: user.id, total_points: 15 }) // Welcome bonus points        .select()        .single();              if (createError) {        console.error("Failed to auto-create user profile:", createError);      } else {        profile = newProfile;                // Log in points ledger        await client.from("points_ledger").insert({          user_id: user.id,          points_awarded: 15,          action_type: "Welcome Bonus"        });      }    }    // Fetch points ledger history    const { data: ledger, error: ledgerError } = await client      .from("points_ledger")      .select("*")      .order("created_at", { ascending: false });    return {      success: true,      profile: profile || { id: user.id, total_points: 0 },      ledger: ledger || [],      user: {        email: user.email,        id: user.id      }    };  } catch (err) {    console.error("Progression server action error:", err);    return { error: "Internal server error" };  }}export async function awardPoints(accessToken, points, actionType) {  if (!accessToken) return { error: "No access token provided" };  try {    const client = await getAuthenticatedClient(accessToken);    const { data: { user }, error: authError } = await client.auth.getUser();        if (authError || !user) return { error: "Unauthorized" };    // Get current profile    const { data: profile } = await client      .from("user_profiles")      .select("total_points")      .eq("id", user.id)      .single();    const newPoints = (profile?.total_points || 0) + Number(points);    // Update profile    const { error: updateError } = await client      .from("user_profiles")      .update({ total_points: newPoints })      .eq("id", user.id);    if (updateError) throw updateError;    // Log ledger entry    const { data: ledgerEntry } = await client      .from("points_ledger")      .insert({        user_id: user.id,        points_awarded: points,        action_type: actionType      })      .select()      .single();    return { success: true, newPoints, ledgerEntry };  } catch (err) {    console.error("Error awarding points:", err);    return { error: err.message };  }}